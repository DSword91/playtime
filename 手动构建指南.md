# PlayTime Tracker - 手动构建指南

## 问题说明

由于网络连接问题，Gradle 无法自动下载 NeoForge 依赖。请按照以下步骤手动完成构建。

---

## 方法一：使用 IntelliJ IDEA（最简单）⭐

IntelliJ IDEA 通常能更好地处理依赖下载。

### 步骤

1. **下载并安装 IntelliJ IDEA**
   - 社区版（免费）：https://www.jetbrains.com/idea/download/

2. **打开项目**
   ```
   File → Open → 选择 mc-playtime-tracker 文件夹
   ```

3. **等待 Gradle 同步**
   - IDEA 会尝试下载所有依赖
   - 如果失败，点击 "Retry" 或 "Reload All Gradle Projects"

4. **配置代理（如果需要）**
   ```
   File → Settings → Build, Execution, Deployment → Build Tools → Gradle
   → HTTP Proxy → 设置你的代理
   ```

5. **构建 JAR**
   - 右侧 Gradle 面板 → Tasks → build → jar
   - 输出：`build/libs/playtimetracker-1.0.0.jar`

---

## 方法二：手动下载依赖后构建

### 步骤 1：下载 NeoForge JAR

访问以下任一地址下载 `neoforge-21.1.65.jar`：

**官方源：**
```
https://maven.neoforged.net/releases/net/neoforged/neoforge/21.1.65/neoforge-21.1.65.jar
```

**备用源（可能需要科学上网）：**
```
https://files.minecraftforge.net/maven/net/neoforged/neoforge/21.1.65/neoforge-21.1.65.jar
```

### 步骤 2：放置依赖文件

将下载的 JAR 文件重命名并放置到：
```
mc-playtime-tracker/lib/neoforge.jar
```

### 步骤 3：运行构建脚本

双击运行：
```
build-complete.bat
```

或在命令行执行：
```bash
cd c:\Users\59485\Desktop\ds\BK\mc-playtime-tracker
build-complete.bat
```

---

## 方法三：使用已配置的 Gradle 缓存

如果你之前在其他项目中已经下载过 NeoForge 依赖，可以复制过来。

### 查找 Gradle 缓存位置

通常在：
```
C:\Users\你的用户名\.gradle\caches\modules-2\files-2.1\net.neoforged\
```

### 复制依赖

找到 `neoforge-21.1.65` 相关的 JAR 文件，复制到项目的 `lib` 文件夹。

---

## 方法四：使用 Docker 构建（高级）

如果你有 Docker 环境，可以使用官方的 NeoForge 开发镜像。

### Dockerfile

创建 `Dockerfile.build`：

```dockerfile
FROM eclipse-temurin:21-jdk

WORKDIR /app
COPY . .

RUN apt-get update && apt-get install -y curl
RUN curl -L -o lib/neoforge.jar \
    https://maven.neoforged.net/releases/net/neoforged/neoforge/21.1.65/neoforge-21.1.65.jar

RUN ./gradlew build --no-daemon

CMD ["cp", "build/libs/playtimetracker-1.0.0.jar", "/output/"]
```

### 构建命令

```bash
docker build -f Dockerfile.build -t pt-builder .
docker run -v $(pwd)/output:/output pt-builder
```

---

## 验证构建结果

构建成功后，你应该在 `build/libs/` 目录下看到：

```
playtimetracker-1.0.0.jar    (约 50-100 KB)
```

### 检查 JAR 内容

```bash
jar tf build/libs/playtimetracker-1.0.0.jar
```

应该包含：
```
META-INF/
META-INF/MANIFEST.MF
META-INF/neoforge.mods.toml
com/bk/playtimetracker/PlayTimeTrackerMod.class
com/bk/playtimetracker/DataManager.class
com/bk/playtimetracker/PlayerActiveData.class
com/bk/playtimetracker/ActiveTimeTracker.class
com/bk/playtimetracker/PlayTimeCommand.class
com/bk/playtimetracker/HttpApiServer.class
com/bk/playtimetracker/PluginConfig.class
```

---

## 快速测试

构建完成后，你可以：

1. **本地测试**
   - 安装 NeoForge 1.21.1 客户端
   - 将 JAR 放入 `mods` 文件夹
   - 启动游戏

2. **服务器测试**
   - 安装 NeoForge 1.21.1 服务器
   - 将 JAR 放入 `mods` 文件夹
   - 启动服务器
   - 检查日志确认 API 已启动

---

## 常见问题

### Q: 为什么无法下载依赖？

A: 可能是网络问题或防火墙阻止。尝试：
- 使用代理
- 切换网络
- 使用国内镜像

### Q: 能否提供预编译的 JAR？

A: 由于版权原因，我不能直接分发包含 NeoForge 代码的 JAR 文件。你需要自己构建。

### Q: 构建需要多长时间？

A: 
- 首次构建（含下载依赖）：5-15 分钟
- 后续构建：1-2 分钟

### Q: 文件大小是多少？

A: 最终 JAR 约 50-100 KB（不包含 NeoForge 依赖，因为它是 provided scope）

---

## 联系支持

如果仍然无法构建，请：

1. 确保你有稳定的网络连接
2. 尝试使用代理或 VPN
3. 在你的 Minecraft 社区寻求帮助
4. 考虑使用其他类似的模组

---

**祝你成功！** 🎮
