# PlayTime Tracker - 快速开始指南

## 📦 项目说明

这是一个 **NeoForge 1.21.1** 模组，用于追踪 Minecraft 玩家的**活跃时间**（排除 AFK 挂机时间）。

### ✨ 核心功能

- ✅ **智能 AFK 检测** - 只记录真实活动时间
- ✅ **多维度活动监测** - 移动、交互、战斗、聊天
- ✅ **HTTP API** - RESTful 接口供外部查询
- ✅ **游戏内命令** - `/playtime` 查看时间
- ✅ **数据持久化** - JSON 文件存储

---

## 🚀 快速使用（3步）

### 步骤 1：构建模组

**推荐方式：使用 IntelliJ IDEA**

```
1. 打开 IntelliJ IDEA
2. File → Open → 选择 mc-playtime-tracker 文件夹
3. 等待 Gradle 同步完成（首次需要几分钟）
4. 右侧 Gradle 面板 → Tasks → build → jar
5. 输出文件：build/libs/playtimetracker-1.0.0.jar
```

### 步骤 2：安装到服务器

```bash
# 1. 确保服务器已安装 NeoForge 1.21.1
# 2. 复制 JAR 文件到 mods 文件夹
cp build/libs/playtimetracker-1.0.0.jar /path/to/server/mods/

# 3. 启动服务器
./run.sh  # Linux
run.bat   # Windows
```

### 步骤 3：验证安装

```bash
# 检查日志
tail -f logs/latest.log | grep "PlayTime"

# 应该看到：
# [PlayTime Tracker] HTTP API 服务器已启动在端口 25002
```

---

## 🎮 使用方法

### 游戏内命令

```bash
/playtime                    # 查看自己的活跃时间
/playtime leaderboard        # 查看排行榜前10名
/playtime leaderboard 20     # 查看前20名
```

### HTTP API

```bash
# 获取排行榜
curl http://localhost:25002/playtime?top=10

# 响应示例
{
  "total_players": 50,
  "leaderboard": [
    {
      "uuid": "...",
      "name": "Player1",
      "play_minutes": 7230
    }
  ]
}
```

---

## 🔧 与 QQ 机器人集成

修改 `hf/napcat-plugin-mcquery` 配置：

```json
{
  "playtimeApiUrl": "http://mc.myluck.top:25002",
  "playtimeCommand": "在线排行"
}
```

然后在 QQ 群发送 `在线排行` 即可查询。

---

## ⚙️ 配置说明

配置文件：`playtime_tracker_config.json`

```json
{
  "apiPort": 25002,           // API 端口
  "afkThresholdTicks": 100,   // AFK 阈值（5秒）
  "movementThreshold": 0.1,   // 移动阈值（方块）
  "saveInterval": 6000,       // 保存间隔（5分钟）
  "apiKey": ""                // API 密钥（可选）
}
```

---

## 📊 活跃判定逻辑

玩家被视为**活跃**的条件（满足任一）：

1. **移动** - 位置变化 > 0.1 方块
2. **交互** - 放置/破坏方块、使用物品
3. **战斗** - 攻击实体、受到伤害
4. **聊天** - 发送消息
5. **维度切换** - 传送等

**AFK 判定：** 5 秒（100 ticks）内无任何活动 → 标记为 AFK

---

## 🛠️ 故障排查

| 问题 | 解决方法 |
|------|----------|
| 模组未加载 | 确认使用 NeoForge 1.21.1 |
| API 无法访问 | 检查防火墙和端口设置 |
| 数据不更新 | 检查玩家是否真的有活动 |
| 编译失败 | 使用 IntelliJ IDEA 自动处理依赖 |

---

## 📁 项目结构

```
mc-playtime-tracker/
├── src/main/java/com/bk/playtimetracker/
│   ├── PlayTimeTrackerMod.java      # 主入口
│   ├── ActiveTimeTracker.java       # 核心追踪器
│   ├── DataManager.java             # 数据管理
│   ├── HttpApiServer.java           # HTTP API
│   └── PlayTimeCommand.java         # 游戏命令
├── build.gradle                      # Gradle 配置
├── INSTALL.md                        # 详细安装指南
└── README.md                         # 完整文档
```

---

## 💡 提示

1. **首次使用**：建议先用 `/playtime` 测试命令是否正常
2. **数据安全**：定期备份 `playtime_data.json` 文件
3. **性能优化**：模组开销极低，不会影响服务器性能
4. **API 安全**：生产环境建议设置 `apiKey` 启用认证

---

## 📞 需要帮助？

查看详细文档：
- `INSTALL.md` - 安装和配置详解
- `README.md` - 完整使用说明
- `PROJECT-OVERVIEW.md` - 技术架构文档

---

**祝游戏愉快！** 🎮
